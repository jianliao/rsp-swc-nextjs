import styles from './ColorOverride.module.css';

export default function ColorOverride({ children }: { children: any }) {
  return (
    <div className={styles.colorOverride052022}>
      {children}
    </div>
  )
}
